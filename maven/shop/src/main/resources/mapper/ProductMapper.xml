<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shop.mapper.ProductMapper">
    <select id="selectByMerchantIdPage" resultType="com.example.shop.entity.Product">
        SELECT * FROM product
        WHERE merchant_id = #{merchamtId}
        ORDER BY product_id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="selectByNamePage" resultType="com.example.shop.entity.Product">
        SELECT * FROM product
        WHERE product_name LIKE CONCAT('%', #{name}, '%')
        ORDER BY product_id DESC
        LIMIT #{offset}, #{limit}
    </select>
</mapper>